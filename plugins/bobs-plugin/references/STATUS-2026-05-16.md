# 진행 상황 — v2 사이클 종료

생성: 2026-05-16
대상: 본 디렉토리(`agent-skill-best-practices/`) 의 현재 상태 요약

---

## 1. 사이클 개요

| 사이클 | 산출 | 위치 |
|---|---|---|
| v1 | 초기 헌법·3 가이드·GAP-FORMAT·GAP-ANALYSIS-PROMPT + v1 자산 분석 7건 | `v1/` archive |
| v2 | v1 기반으로 헌법·3 가이드·GAP-FORMAT·GAP-ANALYSIS-PROMPT 재작성, 자산 inventory 갱신, GAP 분석 15건 | root + `v2/gaps/` |

v2 가이드는 root 에 격상되어 active 문서가 되었고, v2 사이클 분석 결과는 `v2/gaps/` 에 보관된다.

---

## 2. 자산 inventory (v2 사이클 기준)

`v2/v2-update.md` 시점 설치 상태로 스냅샷됨.

| 자산 유형 | 개수 | 비고 |
|---|---:|---|
| Skills | 8 | brainstorming, claude-automation-recommender, claude-md-improver, frontend-design, skill-creator, using-git-worktrees, writing-plans, writing-skills |
| Agents | 4 + builtin | code-simplifier, pr-review-toolkit/{code-reviewer, code-simplifier, comment-analyzer} (builtin: claude-code-guide, general-purpose, Explore, Plan) |
| Hooks | 3 | superpowers SessionStart, ralph-loop Stop, security-guidance PreToolUse |

---

## 3. v2 GAP 분석 결과 요약

### Final Decision 분포 (15건)

| Decision | 수 | 자산 |
|---|---:|---|
| `PASS_WITH_NOTES` | 11 | 8개 skill 전부 + pr-toolkit/code-reviewer + pr-toolkit/comment-analyzer + superpowers/session-start + ralph-loop/stop-hook |
| `REVISE_ASSET` | 2 | code-simplifier (root), security-guidance |
| `SPLIT_ASSET` | 1 | pr-review-toolkit/code-simplifier |

P0 finding 없음.

### 주요 P1 finding

1. **code-simplifier 중복 routing ambiguity** — 루트 `agents/code-simplifier.md` 와 `agents/pr-review-toolkit/code-simplifier.md` 가 `name` 동일·본문 한 단락도 다르지 않음. 루트 REVISE, toolkit SPLIT.
2. **code-simplifier 자동 mutation gate 없음 + `tools` 미명시** — "operate autonomously and proactively, refining code immediately" 가 사용자 승인 없이 mutation 가능 경로.
3. **security-guidance advisory 가 hard block 으로 운영** — HOOK-GUIDE §1/§7/§13 anti-pattern 그대로. 본 분석 작성 중 substring 매칭으로 Write tool 2회 차단됨.

---

## 4. v2 사이클에서 헌법·가이드에 반영된 변경

### HOOK-GUIDE.md 패치 (v2 GAP 분석 결과 반영)

- §10.5 Auto-Loop Trigger 패턴 (opt-in / cap / completion gate / visibility / stop path)
- §10.6 Session-Scoped State Files (user scope / 단일 store / 명시적 lifecycle / absolute path)
- §13 Anti-Patterns 4행 추가:
  - Advisory hook configured as blocker
  - Substring rule as hard block
  - Unbounded auto-loop
  - Scattered session state
- §14 Version-Sensitive Details 확장: `additional_context` vs `additionalContext`, Stop hook `decision/reason/systemMessage`, PreToolUse 차단 exit code, `# verified against: <runtime> <version>` 주석 권장

### CONSTITUTION / SKILL-GUIDE / AGENT-GUIDE

변경 없음. v2 GAP 분석에서 `Constitution Review` 후보 0건, SKILL-GUIDE / AGENT-GUIDE 의 `GUIDE_GAP` 후보는 자산 수정으로 해결 가능한 범위로 분석가가 판정.

---

## 5. 외부 모델 리뷰 반영

본 사이클에서 GPT 5.5 가 GAP 리포트 6건의 품질 이슈를 지적했고 모두 반영됨:

1. `hook-ralph-loop-stop-hook.GAP.md` GAP-003 P3 → P2, `set -e` 분석 정정, jq fallback 누락 사실 명시
2. 같은 리포트 Input handling check pass → partial
3. `hook-security-guidance-security-reminder.GAP.md` Guide Changes 에서 GAP-FORMAT cross-reference 항목 제거 (권위 방향 역행)
4. 같은 리포트 GAP-006 reference `CONSTITUTION §3.4` → `§3.3 / §3.5 + HOOK-GUIDE §8/§9`
5. `skill-using-git-worktrees.GAP.md` Highest severity P2 → P3
6. `skill-claude-automation-recommender.GAP.md` 요약 문구를 finding 표와 정합

---

## 6. 후속 follow-up (자산 측 미해결)

GAP 분석에서 ASSET_GAP 으로 분류되었으나 본 사이클에서는 보고만 되고 자산 수정은 진행되지 않은 항목:

- `code-simplifier` 중복 (루트 vs pr-toolkit) — 통합 또는 toolkit 측 차별화 필요
- `code-simplifier` 양쪽 자동 mutation gate 추가, `tools` 명시
- `security-guidance` advisory 모드 기본화, substring 룰 강등, state file lifecycle 정리
- `writing-skills` ↔ `skill-creator` overlap 의도 확인
- `writing-plans` 의 `docs/superpowers/plans/` 기본 경로 + `superpowers:*` sub-skill 의존성 검토
- `ralph-loop/stop-hook.sh` 의 grep/jq fallback 누락 (P2)

자산은 본 디렉토리의 reference snapshot 이며 원본은 각 플러그인이 소유한다. 실제 수정은 플러그인 측에 제안하거나 fork 가 필요.

---

## 7. 다음 사이클 (v3) 진입 조건

- 자산 inventory 가 크게 바뀌었을 때 (플러그인 추가/제거, 주요 버전 업데이트)
- 헌법 또는 가이드에 적용해야 할 새 원칙이 누적되었을 때
- 본 사이클의 P1 finding 들이 자산 측에서 해결되어 재검증이 필요할 때

진입 방법: `v3/` 디렉토리 생성 → root 가이드 그대로 사용 → `v3/gaps/` 만 새로 생성 → `GAP-ANALYSIS-PROMPT.md` 의 출력 경로만 `v3/gaps/` 로 조정.
